[package]
name = "thx-dsp"
description = "THX DSP framework and processors."
version.workspace = true
edition.workspace = true
repository.workspace = true

[dependencies]
bitflags = "2.13"

[dev-dependencies]
assert_no_alloc = "1.1"
